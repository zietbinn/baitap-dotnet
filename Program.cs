// using System;
//      namespace myproject
//      {
//           class project
//           {
//                static void Main()
//                {
                    // câu 1:

                    // Console.WriteLine("Nhập kích thước của mảng:");
                    //      int n = int.Parse(Console.ReadLine());
                    //      int[] arr = new int[n];
                    //      Console.WriteLine("Nhập các phần tử của mảng:");
                    //      for (int i = 0; i < n; i++)
                    //      {
                    //           Console.Write($"Phần tử {i + 1}: ");
                    //           arr[i] = int.Parse(Console.ReadLine());
                    //      }
                    //      int sum =0;
                    //      for (int i = 0; i < n; i++){
                    //           sum+=arr[i];
                    //      }
                    //      Console.Write($"Tổng các phần tử: {sum} ");
                    
                    // --------------------------------------------------------------------
                    // --------------------------------------------------------------------

                    // câu 2:

                    // Console.Write("Nhập chuỗi: ");
                    // string input = Console.ReadLine();
                    // int count = 0;
                    // for (int i = 0; i < input.Length; i++)
                    // {
                    //      char c = input[i];
                    //      if (!char.IsWhiteSpace(c) && !char.IsPunctuation(c))
                    //      {
                    //           count++;
                    //      }
                    // }
                    // Console.WriteLine("Số lượng ký tự trong chuỗi (không tính khoảng trắng và dấu câu): " + count);

                    // --------------------------------------------------------------------
                    // --------------------------------------------------------------------

                    // câu 3:

                    // Console.WriteLine("Nhập kích thước của mảng:");
                    // int n = int.Parse(Console.ReadLine());
                    // int[] arr = new int[n];
                    // Console.WriteLine("Nhập các phần tử của mảng:");
                    // for (int i = 0; i < n; i++)
                    // {
                    //      Console.Write($"Phần tử {i + 1}: ");
                    //      arr[i] = int.Parse(Console.ReadLine());
                    // }
                    // int max = arr[0];
                    // for (int i = 0; i < n; i++){
                    //      if( arr[i] > max){
                    //           max = arr[i];
                    //      }
                    // }
                    // Console.WriteLine($"Phần tử lớn nhất trong mảng là:{max}");

                    // --------------------------------------------------------------------
                    // --------------------------------------------------------------------

                    // câu 4:

                    // Console.Write("Nhập chuỗi: ");
                    // string input = Console.ReadLine();
                    // char[] charArray = input.ToCharArray();
                    // Array.Reverse(charArray);
                    // string reversed = new string(charArray);
                    // Console.WriteLine("Chuỗi sau khi đảo ngược: " + reversed);

                    // Console.Write("Nhập chuỗi: ");
                    // string input = Console.ReadLine();
                    // string reversed = "";
                    // for (int i = input.Length - 1; i >= 0; i--)
                    // {
                    //      reversed += input[i];
                    // }
                    // Console.WriteLine("Chuỗi sau khi đảo ngược: " + reversed);

                    // --------------------------------------------------------------------
                    // --------------------------------------------------------------------

                    // câu 5:

                    // Console.WriteLine("Nhập kích thước của mảng:");
                    // int n = int.Parse(Console.ReadLine());
                    // int[] arr = new int[n];
                    // Console.WriteLine("Nhập các phần tử của mảng:");
                    // for (int i = 0; i < n; i++)
                    // {
                    //      Console.Write($"Phần tử {i + 1}: ");
                    //      arr[i] = int.Parse(Console.ReadLine());
                    // }
                    // bool isPalindrome = true;
                    // for (int i = 0; i < n / 2; i++)
                    // {
                    //      if (arr[i] != arr[n - i - 1])
                    //      {
                    //           isPalindrome = false;
                    //           break;
                    //      }
                    // }
                    // if (isPalindrome)
                    // {
                    //      Console.WriteLine("Mảng là đối xứng (Palindrome).");
                    // }
                    // else
                    // {
                    //      Console.WriteLine("Mảng không phải là đối xứng.");
                    // }

                    // --------------------------------------------------------------------
                    // --------------------------------------------------------------------

                    // câu 6:
                    // Console.Write("Nhập chuỗi: ");
                    // string input = Console.ReadLine();
                    // Console.Write("Nhập ký tự cần đếm: ");
                    // char characterToCount = char.Parse(Console.ReadLine());
                    // int count = 0;
                    // for (int i = 0; i < input.Length; i++)
                    // {
                    //      if (input[i] == characterToCount)
                    //      {
                    //      count++;
                    //      }
                    // }
                    // Console.WriteLine($"Ký tự '{characterToCount}' xuất hiện {count} lần trong chuỗi.");
     //           }
     //      }
     // }












